{
  "name": "machine-cafe-tests",
  "version": "1.0.0",
  "description": "Tests pour la machine à café",
  "main": "machine-cafe.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "collectCoverageFrom": [
      "*.js",
      "!*.test.js"
    ]
  }
}